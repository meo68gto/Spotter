{
  "name": "@spotter/env",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "echo 'No tests yet for @spotter/env'"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
